# Requirements for running notebooks/explore.ipynb (and the rest of the
# project) outside of the uv-managed dev environment.
#
# Install from the repo root:
#   pip install -r requirements.txt
#
# This pulls every runtime dependency declared in pyproject.toml (via the
# editable install of the `eia` package itself) plus the notebook-specific
# extras (jupyterlab, matplotlib, ipykernel).
#
# For uv-managed setup, use `make install-dev` (and `make install-notebook`)
# instead — they read pyproject.toml directly.

# --- Local package (pulls in all runtime deps from pyproject.toml) ---
-e .

# --- Runtime deps (kept explicit so this file documents the surface) ---
duckdb>=1.1.0
polars>=1.12.0
pyarrow>=17.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0
tenacity>=9.0.0
pyyaml>=6.0.0
typer>=0.12.0
rich>=13.9.0
geopandas>=1.0.0
requests>=2.32.0
fastexcel>=0.20.2

# --- Notebook deps ---
jupyterlab>=4.2.0
matplotlib>=3.9.0
ipykernel>=7.0.0
awswrangler>=3.16.1
boto3>=1.43.10
pandas>=3.0.3


# --- Optional dev deps (uncomment to enable tests/lint locally) ---
# sagemaker
# pytest>=8.3.0
# pytest-cov>=5.0.0
# ruff>=0.7.0
# mypy>=1.13.0
# openpyxl>=3.1.0
# types-pyyaml>=6.0.0
